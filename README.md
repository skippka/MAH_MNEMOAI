## 🧠 МнемоШІ — веб‑тренер пам’яті з ІІ (Flask + Gemini)

**МнемоШІ** — це веб‑додаток на Flask, який допомагає запам’ятовувати навчальні тексти за допомогою:
- **локального генератора мнемонік** (`ai_model.py`, працює офлайн);
- **Google Gemini** (режим «глибоке мислення», опційно, якщо налаштований API‑ключ).

Ви можете:
- вставити текст або завантажити файл (TXT/PDF/DOCX);
- отримати аналіз тексту (ключові фрази, теми, складність);
- згенерувати мнемоніки (акроніми, історії, рими, візуальні образи тощо);
- отримати план навчання і поради для запам’ятовування;
- створити невеликий тест‑квіз по тексту.

---

## ✨ Основні можливості

- **Веб‑інтерфейс на Flask**
  - Сторінка введення/завантаження тексту (`upload.html`).
  - Сторінка з результатами та мнемоніками (`result.html`).
- **2 режими роботи з текстом**
  - **Звичайне мислення (`mode=normal`)** — все рахується локально (без інтернету).
  - **Глибоке мислення (`mode=deep`)** — повний аналіз і мнемоніки від Google Gemini (потрібен API‑ключ).
- **Багато технік мнемоніки (локальна модель)**
  - Акроніми, акростихи, рими, історії, палац пам’яті, візуальні асоціації, числові коди, метафори, групування тощо.
- **AI‑підсилення**
  - Режим «Gemini допоможе з текстом» — красиве переписування тексту.
  - Повний AI‑план навчання та поради у режимі `deep`.

---

## 🚀 Як запустити проєкт локально

### 1. Клонування та перехід в папку

```bash
git clone <URL_ВАШОГО_РЕПОЗИТОРІЮ>
```

### 2. Віртуальне оточення (рекомендовано)

```bash
python -m venv .venv
.venv\Scripts\activate  # Windows
```

### 3. Встановлення залежностей

```bash
pip install -r requirements.txt
```

### 4. Налаштування Google Gemini (опційно, для «глибокого мислення»)

У **Windows PowerShell**:

```powershell
$env:GEMINI_API_KEY = "ВАШ_API_КЛЮЧ"
# опційно: конкретна модель (за замовчуванням використовується gemini-1.5-flash)
$env:GEMINI_MODEL = "gemini-1.5-flash"
```

- якщо `GEMINI_API_KEY` не заданий, режим `deep` автоматично переходить на звичайний локальний режим;
- локальні мнемоніки й аналіз працюють **без** API‑ключа.

### 5. Запуск Flask‑додатку

```bash
python app.py
```

Після цього відкрийте у браузері:

- `http://127.0.0.1:5000/` — головна сторінка;
- `http://127.0.0.1:5000/upload` — форма для тексту/файлів.

---

## 🗂 Структура проєкту

- `app.py` — основний Flask‑сервер, API‑ендпоїнти:
  - `/api/process_text` — аналіз тексту, генерація мнемонік (normal/deep);
  - `/api/upload_file` — завантаження файлів і обробка;
  - `/api/gemini_help` — покращення тексту через Gemini;
  - `/api/quiz` — генерація тесту;
  - `/api/generate_story` — генерація історії з ключових слів.
- `ai_model.py` — локальний генератор мнемонік (багато технік).
- `utils.py` — `TextProcessor` (очистка тексту, ключові фрази, теми, складність).
- `gemini_client.py` — обгортка над Google Gemini.
- `templates/`
  - `index.html` — головна сторінка.
  - `upload.html` — введення/завантаження тексту.
  - `result.html` — виведення мнемонік, плану і порад.
- `static/user_data/` — JSON‑файли з результатами сесій (можна додати в `.gitignore`).
- `uploads/` — завантажені файли користувача (також бажано ігнорувати в Git).

---

## ⚙️ Рекомендований `.gitignore`

```gitignore
__pycache__/
.venv/
uploads/
static/user_data/
*.pyc
*.log
```

---

## 🧩 Використання локального генератора мнемонік напряму

Якщо потрібно використати тільки логіку генерації, без веб‑частини:

```python
from ai_model import MnemonicGenerator

generator = MnemonicGenerator()

key_phrases = [
    "Економічна функція",
    "Соціальна відповідальність",
    "Інноваційний розвиток",
    "Ресурсне забезпечення",
]
main_topics = ["Функції підприємства"]

mnemonics = generator.generate_mnemonics(key_phrases, main_topics)

print(mnemonics["acronyms"])  # Акроніми
print(mnemonics["stories"])   # Історії
print(mnemonics["rhymes"])    # Рими
```

Також доступні методи:
- **`analyze_text_complexity(text)`** — оцінка складності тексту;
- **`generate_quiz(text)`** — простий тест по тексту;
- **`get_memory_tips()`** — випадкові поради для пам’яті;
- **`create_comprehensive_plan(text, key_phrases)`** — план з 5 фаз навчання.

---

## 📄 Ліцензія

Проєкт можна вільно використовувати та розширювати для особистих навчальних цілей. При публікації форку буде приємно, якщо залишите посилання на оригінальний репозиторій.

---

## 🙏 Подяка

Дякуємо за використання **МнемоШІ**! Нехай мнемоніки реально допомагають краще запам’ятовувати складні тексти та готуватися до іспитів. 🎓

